from multiprocessing import connection
import pandas as pd
import numpy as np


import sqlalchemy
database_username = 'admin'
database_password = 'y$UYRPB&7Go5'
database_host = 'database-1.cfdamesx7fal.ap-southeast-1.rds.amazonaws.com'
database_name = 'tuku_pos
database connection = sqlalchemy.create_engine('mysql+mysqlconnector://{0}:{1} @{2}/{3})'.
                                                format(database_admin, database_y$UYRPB&7Go5, database_database-1.cfdamesx7fal.ap-southeast-1.rds.amazonaws.com))
frame.to_sql(con=database_conecction, name='tuku_pos', if_exists='replace)'

np random.seed(0)
number_of_sample = 10
frame  = pd.test_dwh({
    'DATA_CREATE' : np.random.random(number_of_sample)
    'TRANS_DATE' : np.random.random(number_of_sample)
}, columns= ['DATA_CREATE', 'TRANS_DATE'])

Print(frame)
